const categoryEl = document.querySelectorAll('li.item');
console.log(`Number of categories: ${categoryEl.length}`);

const elemUl = document.querySelector('.animal');
console.log(`Category: ${elemUl.textContent}`);

const listAnimalEl = document.querySelectorAll('.animals__item');
console.log(`Elements: ${listAnimalEl.length}`);